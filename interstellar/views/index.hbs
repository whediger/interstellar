<header>
  <nav>
    <div class="nav-div">

    </div>
    <div class="nav-div">
      <h1>Interstellar Propulsion</h1>
    </div>
    <div class="nav-end">
      <a href="/create">Add a propulsion Type</a>
    </div>


  </nav>

</header>

<main>



  {{#each list}}
  <div class="container">
    <a href="{{this.id}}"><h2>{{this.title}}</h2></a>
    <img class="image" src="{{this.image}}" alt="" />
    <p>{{this.description}}</p>
    </div>
  {{/each}}

</main>
